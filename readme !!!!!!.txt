read me !!!!

please install Pillow library to ensure that the code shows the output image AND install pyamaze to ensure that the code runs properly the GUI's font may seem strange but if a font named gameplay installed the gui will look better 
and install tkinter library for running the gui , import sys , priority queue from a library named queue 
when running the code please select any maze from the attached txt files 